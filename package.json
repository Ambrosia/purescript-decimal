{
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp test && rimraf docs && pulp docs"
  },
  "dependencies": {
    "decimal.js": "^7.2.0"
  },
  "devDependencies": {
    "pulp": "^9.0.0",
    "rimraf": "^2.5.2"
  }
}
